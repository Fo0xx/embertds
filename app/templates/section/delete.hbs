{{page-title "Delete"}}
  <div class="ui grid">
    <div class="column two wide"></div>
    <div class="column twelve wide">
      <fieldset>
        <legend>&nbsp;Delete a section&nbsp;</legend>
        <form class="ui form">
          <br>
          <h2 class="ui center aligned header dividing">Delete Section : {{ @model.name }}</h2>
          <br>

          <div class="ui grid">
            <div class="sixteen wide column" style="text-align: center">
              <button class="ui button red basicajax _connect floated" {{ action "deleteSection" @model }}>Delete {{ @model.name }}</button>
              <br><br>
              <LinkTo class="ui button grey basic ajax _connect floated" @route="section">Close</LinkTo>
            </div>
          </div>
        </form>
      </fieldset>
    </div>
  </div>
{{outlet}}